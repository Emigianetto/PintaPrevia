    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">Pinta Previa</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">

          <%= form_for :users, url: url_for( :controller => :users, :action => :login ), :html => {:class => "navbar-form navbar-right"} do |f| %>
            <div class="form-group">
              <%= f.email_field :email, placeholder: "Mail", class: "form-control" %>
            </div>
            <div class="form-group">
              <%= f.password_field :pass, placeholder: "Clave", class: "form-control" %>
            </div>
            <%= button_tag(type: 'submit', class: "btn btn-success btn-md") do %>
              Ingresar
            <% end %>
          <% end %>
        </div><!--/.navbar-collapse -->
      </div>
    </nav>