<!DOCTYPE html>
<html lang="en">
<head>
  <title>PintaPrevia</title>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="Pinta Previa">
  <meta name="author" content="Emiliano Gianetto">
    

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <!-- <link href="/assets/images/favicon.ico" rel="shortcut icon" /> -->
  <!-- <link href="/assets/images/favicon.ico" rel="shortcut icon" type="image/x-icon" /> -->
  <%= favicon_link_tag %>
</head>
<body style="padding-top: 70px">
  <%= render "partials/topnav_login" %>
<% if false %>
  <% if logged_in? %>
    <%= render "partials/topnav_logout" %>
  <% else %>
    <%= render "partials/topnav_login" %>
  <% end %>
<% end %>
  <div class="container-fluid">
    <div class="row">
      <div class="col-sm-3 col-md-2 sidebar">
        <%= render "partials/sidenav_user" %>
      <% if false %>
        <% if admin? %>
          <%= render "partials/sidenav_admin" %>
        <% else %>
          <%= render "partials/sidenav_user" %>
        <% end %>
      <% end %>
      </div>
      <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
        <%= yield %>
      </div>
    </div>
    <%= render "partials/footer" %>
  </div>
</body>
</html>
